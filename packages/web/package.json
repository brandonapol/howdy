{
  "name": "@howdy/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --build && vite build",
    "preview": "vite preview",
    "test": "tsc --build && node --test --experimental-strip-types test/state.test.ts",
    "typecheck": "tsc --build",
    "test:e2e": "npm run build && node --test --test-timeout=60000 --experimental-strip-types test/ui.e2e.test.ts"
  },
  "dependencies": {
    "@howdy/core": "*",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "playwright": "^1.63.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
